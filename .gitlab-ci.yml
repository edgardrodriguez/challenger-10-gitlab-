sonarqube-check:
  image: maven:3.6.3-jdk-8-openj9
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script: 
    - mvn sonar:sonar -Dsonar.projectKey=AS201S3_T11_SistCom -Dsonar.host.url=http://34.68.32.239:9000 -Dsonar.login=622c3d02a310caf99f39ffc07ebba192a27796e1
  allow_failure: true
  only:
    - SistCom_Maven
